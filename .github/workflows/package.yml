on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:  # hier kannst du dann manuell triggern!

jobs:
  rpm:
    uses: lbetz/ci-workflows/.github/workflows/rpm-build.yml@main
    with:
      ci_workflows_ref: "main"
      distros: '["fedora44", "el9"]'
      architectures: '["x86_64", "aarch64"]'
